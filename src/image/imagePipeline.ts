import type{Line}from'../types';
export async function rasterToLines(file:File,detail:'low'|'balanced'|'high'='balanced'):Promise<Line[]>{const url=URL.createObjectURL(file);try{const img=await createImageBitmap(file);const max=1200,scale=Math.min(1,max/Math.max(img.width,img.height));const w=Math.max(1,Math.round(img.width*scale)),h=Math.max(1,Math.round(img.height*scale));const c=new OffscreenCanvas(w,h),ctx=c.getContext('2d',{willReadFrequently:true})!;ctx.drawImage(img,0,0,w,h);const d=ctx.getImageData(0,0,w,h),g=new Uint8Array(w*h);for(let i=0,p=0;i<d.data.length;i+=4,p++){g[p]=Math.round(.299*d.data[i]+.587*d.data[i+1]+.114*d.data[i+2])}const threshold=detail==='low'?150:detail==='high'?190:170;const pts:{x:number;y:number}[]=[];for(let y=1;y<h-1;y+=2)for(let x=1;x<w-1;x+=2){const i=y*w+x;if(g[i]<threshold){let n=0;for(let yy=-1;yy<=1;yy++)for(let xx=-1;xx<=1;xx++)if(g[(y+yy)*w+x+xx]<threshold)n++;if(n>=2)pts.push({x:x/w-.5,y:y/h-.5})}}if(pts.length<2)return[];const stride=Math.max(1,Math.floor(pts.length/500));const sampled=pts.filter((_,i)=>i%stride===0);return[{id:crypto.randomUUID(),points:sampled,width:3.5}]}finally{URL.revokeObjectURL(url)}}