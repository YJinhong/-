import type{Line,Point}from'../types';
function thin(src:Uint8Array,w:number,h:number){const a=new Uint8Array(src);let changed=true;const idx=(x:number,y:number)=>y*w+x;while(changed){changed=false;for(let pass=0;pass<2;pass++)for(let y=1;y<h-1;y++)for(let x=1;x<w-1;x++){const i=idx(x,y);if(!a[i])continue;const p=[a[idx(x,y-1)]>0,a[idx(x+1,y-1)]>0,a[idx(x+1,y)]>0,a[idx(x+1,y+1)]>0,a[idx(x,y+1)]>0,a[idx(x-1,y+1)]>0,a[idx(x-1,y)]>0,a[idx(x-1,y-1)]>0],n=p.filter(Boolean).length,t=p.reduce((s,v,j)=>s+(!p[j-1<0?7:j-1]&&v?1:0),0),m=pass===0?(p[0]&&p[2]&&p[4])||(p[2]&&p[4]&&p[6]):(p[0]&&p[2]&&p[6])||(p[0]&&p[4]&&p[6]);if(n>=2&&n<=6&&t===1&&!m){a[i]=0;changed=true}}}return a}
function components(mask:Uint8Array,w:number,h:number){const seen=new Uint8Array(mask.length),out:Point[][]=[];for(let y=1;y<h-1;y++)for(let x=1;x<w-1;x++){const start=y*w+x;if(!mask[start]||seen[start])continue;const q=[{x,y}],pts:Point[]=[];seen[start]=1;while(q.length){const p=q.pop()!;pts.push(p);for(let dy=-1;dy<=1;dy++)for(let dx=-1;dx<=1;dx++){if(!dx&&!dy)continue;const nx=p.x+dx,ny=p.y+dy;if(nx<1||nx>=w-1||ny<1||ny>=h-1)continue;const ni=ny*w+nx;if(mask[ni]&&!seen[ni]){seen[ni]=1;q.push({x:nx,y:ny})}}}if(pts.length>=4)out.push(pts)}return out}
function traceComponent(mask:Uint8Array,w:number,h:number,component:Point[]):Point[]{const allowed=new Set(component.map(p=>p.x+','+p.y));let start=component[0];for(const p of component){let degree=0;for(let dy=-1;dy<=1;dy++)for(let dx=-1;dx<=1;dx++)if((dx||dy)&&allowed.has((p.x+dx)+','+(p.y+dy)))degree++;if(degree===1){start=p;break}}const out:Point[]=[];let cur={...start},prev={x:start.x-1,y:start.y};const seen=new Set<string>();for(let k=0;k<component.length;k++){out.push({x:cur.x/w-.5,y:cur.y/h-.5});seen.add(cur.x+','+cur.y);let next:{x:number;y:number}|undefined,best=Infinity;for(let dy=-1;dy<=1;dy++)for(let dx=-1;dx<=1;dx++){if(!dx&&!dy)continue;const q={x:cur.x+dx,y:cur.y+dy};if(!allowed.has(q.x+','+q.y)||seen.has(q.x+','+q.y))continue;const d=(q.x-prev.x)**2+(q.y-prev.y)**2;if(d<best){best=d;next=q}}if(!next)break;prev=cur;cur=next}return out}
function rdp(points:Point[],eps:number):Point[]{if(points.length<3)return points;let max=0,idx=0,a=points[0],b=points.at(-1)!;for(let i=1;i<points.length-1;i++){const d=Math.abs((b.x-a.x)*(a.y-points[i].y)-(a.x-points[i].x)*(b.y-a.y))/(Math.hypot(b.x-a.x,b.y-a.y)||1);if(d>max){max=d;idx=i}}if(max>eps){const l=rdp(points.slice(0,idx+1),eps),r=rdp(points.slice(idx),eps);return l.slice(0,-1).concat(r)}return[a,b]}
export async function rasterToLines(file:File,detail:'low'|'balanced'|'high'='balanced'):Promise<Line[]>{const img=await createImageBitmap(file),max=1000,scale=Math.min(1,max/Math.max(img.width,img.height)),w=Math.max(8,Math.round(img.width*scale)),h=Math.max(8,Math.round(img.height*scale)),c=new OffscreenCanvas(w,h),ctx=c.getContext('2d',{willReadFrequently:true})!;ctx.drawImage(img,0,0,w,h);const d=ctx.getImageData(0,0,w,h),gray=new Uint8Array(w*h);for(let i=0;i<w*h;i++)gray[i]=Math.round(.299*d.data[i*4]+.587*d.data[i*4+1]+.114*d.data[i*4+2]);const t=detail==='low'?145:detail==='high'?185:165,binary=new Uint8Array(w*h);for(let i=0;i<gray.length;i++)binary[i]=gray[i]<t?1:0;const skeleton=thin(binary,w,h),parts=components(skeleton,w,h),eps=detail==='low'?.012:detail==='high'?.004:.008;const minPoints=detail==='low'?12:detail==='high'?4:8;const lines:Line[]=[];for(const part of parts){const pts=traceComponent(skeleton,w,h,part),simp=rdp(pts,eps);if(simp.length>=2&&part.length>=minPoints)lines.push({id:crypto.randomUUID(),points:simp,width:3.5})}return lines.sort((a,b)=>b.points.length-a.points.length)}