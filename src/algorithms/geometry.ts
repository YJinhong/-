import type{Point,Line}from'../types';export const dist=(a:Point,b:Point)=>Math.hypot(a.x-b.x,a.y-b.y);export const length=(p:Point[])=>p.slice(1).reduce((s,b,i)=>s+dist(p[i],b),0);export function simplify(points:Point[],tol:number){if(points.length<3)return points;const keep=[points[0]];for(let i=1;i<points.length-1;i++){const a=keep[keep.length-1],b=points[i],c=points[i+1];const area=Math.abs((b.x-a.x)*(c.y-a.y)-(b.y-a.y)*(c.x-a.x));const base=dist(a,c)||1;if(area/base>tol)keep.push(b)}keep.push(points.at(-1)!);return keep}export function lineBounds(l:Line){const xs=l.points.map(p=>p.x),ys=l.points.map(p=>p.y);return{x:Math.min(...xs),y:Math.min(...ys),w:Math.max(...xs)-Math.min(...xs),h:Math.max(...ys)-Math.min(...ys)}}