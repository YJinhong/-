import type{Line,Point}from'../types';
export type GraphNode={id:string;point:Point;degree:number;lineIds:string[]};
export function buildSkeletonGraph(lines:Line[],radius=.018){const nodes:GraphNode[]=[];const find=(p:Point)=>nodes.findIndex(n=>Math.hypot(n.point.x-p.x,n.point.y-p.y)<=radius);for(const l of lines){for(const p of[l.points[0],l.points.at(-1)!]){let i=find(p);if(i<0){nodes.push({id:crypto.randomUUID(),point:{...p},degree:1,lineIds:[l.id]})}else{nodes[i].degree++;if(!nodes[i].lineIds.includes(l.id))nodes[i].lineIds.push(l.id)}}}return nodes}
export function eulerTrail(lines:Line[]){if(!lines.length)return[];const remaining=new Map(lines.map(l=>[l.id,l]));const result:Line[]=[];let current=lines[0];remaining.delete(current.id);result.push(current);while(remaining.size){const tail=current.points.at(-1)!;let best:Line|undefined,bestD=Infinity;for(const l of remaining.values()){const d=Math.hypot(l.points[0].x-tail.x,l.points[0].y-tail.y);if(d<bestD){bestD=d;best=l}}if(!best){best=remaining.values().next().value}remaining.delete(best.id);result.push(best);current=best}return result}
export function penLifts(order:Line[]){let lifts=0;for(let i=1;i<order.length;i++){const a=order[i-1].points.at(-1)!,b=order[i].points[0]!;if(Math.hypot(a.x-b.x,a.y-b.y)>.08)lifts++}return lifts}