import{openDB}from'idb';import type{Project}from'../types';const db=openDB('sugardraw',1,{upgrade(db){if(!db.objectStoreNames.contains('projects'))db.createObjectStore('projects',{keyPath:'id'})}});export const saveProject=(p:Project)=>db.then(d=>d.put('projects',p));export const listProjects=()=>db.then(d=>d.getAll('projects'));export const deleteProject=(id:string)=>db.then(d=>d.delete('projects',id));